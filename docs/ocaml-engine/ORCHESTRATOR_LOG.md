# Orchestrator log

Append-only. One row per orchestrator action (PORT chunk, FIX, PLAN reconcile, gate).
Never edit or delete prior rows; corrections get a new row referencing the old one.
Each row is appended in the SAME commit as the work it records (commit column = short SHA;
`pending` only transiently before the amend step of /commit).

Columns:

- **timestamp** — UTC, ISO 8601.
- **mode** — `PORT` | `FIX` | `PLAN` | `GATE` | `NOTE`.
- **chunk** — milestone-doc checklist item name, or the fix's frontier test / diagnosis.
- **frontier before → after** — `file:ordinal → file:ordinal` (`—` when not applicable,
  e.g. before the runner exists).
- **commit** — short SHA of the commit carrying the work.
- **notes** — regressions hit, deviations, out-of-scope findings, review rounds, risks.

| timestamp | mode | chunk | frontier before → after | commit | notes |
|---|---|---|---|---|---|
| 2026-07-06T18:15:00Z | NOTE | M0 start: vendored PCRE2 10.44 sources + testdata; porting infrastructure (.claude/ skills, agents, rules; docs/ocaml-engine/ plan docs) authored | — → — | 9b1ab09 | vendor chunk of 01-infrastructure.md complete; harness/runner being built concurrently; next: build restructure chunk toward gate G0 |
| 2026-07-06T19:05:00Z | PORT | M0 build restructure: pcre2-dev package, oracle/ C library (oracle_-prefixed symbols), flake pins C pcre2 at 10.44, ocaml floor 4.14, include_subdirs for engine/ | — → — | 4f8f03f | oracle OUnit suite green 41/41 vs 10.44 (version test was failing vs 10.42 depext); GCC -I/-isystem dedup pitfall documented in flake.nix |
| 2026-07-06T19:10:00Z | PORT | M0 infra commit for .claude/ + docs/ocaml-engine/ (authored at M0 start, committed separately) | — → — | fcb3264 | log rows for 4f8f03f/fcb3264 appended late (infra bootstrapping); from here on, same-commit rule applies |
| 2026-07-06T19:40:00Z | PORT | M0 oracle test driver: pcre2test_stubs.c + pcre2-dev.test-driver (compile ctx, erroroffset, full ovector, MARK, startchar, info, name table, error_message) | — → — | 38ecb9e | smoke-tested: err 114 off 4 on "(abc"; MARK + unset-group ovector correct; harness agent consumes this |
| 2026-07-06T20:30:00Z | PORT | M0 constants: engine/limits.ml, options.ml, opcodes.ml (OP enum 0..170 + op_lengths + op_names, length-asserted), errors.ml (verbatim error texts + message) | — → — | d8e54bc | anchors spot-checked vs pcre2_internal.h (OP_CHAR=29, OP_KET=121, OP_BRA=135, table len 171); masks vs pcre2_compile.c:774-792/pcre2_match.c:73-82; message() mirrors pcre2_get_error_message indexing |
| 2026-07-06T21:20:00Z | PORT | M0 generators + tables: gen/gen_tables.exe (strict parser, --check sanity pins, @gen-check diff alias) -> generated chartables.ml, ucd_tables.ml (nrecords 1423, stage2 39040, Unicode 15.0.0), ucptables.ml (utt 489); hand-ported ucp.ml (295 consts), tables.ml | — → — | 43775d5 | Kelvin sign other_case=0 (cased via caseless set 100) noted; utt loose-matched names ("greek", "l&"); dune build -p pcre2 unaffected |
| 2026-07-06T21:55:00Z | PORT | M0 engine skeleton + pure swap: pcre2.engine sub-library (stdlib-only), engine.ml/.mli boundary (skeleton fails with ERR23/-44), bindings.ml rewritten as pure shim (names/types frozen), C stubs + config/ + conf-libpcre2-8 + dune-configurator removed from package pcre2, pure OUnit moved to @pure-tests until G1 | — → — | c97324d | dune build -p pcre2 green with no C; runtest = oracle suite 41/41; pcre2.ml/.mli/intf.ml zero-diff as required |
| 2026-07-06T22:40:00Z | PORT | M0 harness + runner: pcre2test-compatible harness (functor over DRIVER; full 140-entry modlist; process_data escape decoder; global-loop with NOTEMPTY_ATSTART retry + CRLF/UTF bump; copy/get/getall; MK:/partial/aftertext/allcaptures) + conformance runner (unit splitter/aligner, skiplist w/ STALE-SKIP, baselines, --frontier/--only) | — → testinput2:1305 | 75e5352 | testinput1 1290/1290 BYTE-IDENTICAL vs oracle; sweep: t4 617/617, t5 418P/0F/206S, t2 729P/12F/1102S; 12 failures diagnosed: 7 callout units, 4 MARK-with-NUL (driver strlen bug), 1 RunTest -error artifact |
| 2026-07-06T23:10:00Z | GATE | G0 MET: all 7 testdata files byte-identical vs C oracle outside skiplist (3112 in-scope units: t1 1290, t2 733, t4 617, t5 418, t8 0, t9 10, t10 44); MARK driver bug fixed (length prefix at mark[-1], pcre2test.c:8131/3099); skiplist: 7 callout units out-of-scope + 1 env RunTest artifact | testinput2:1305 → none failing | 326a8e2 | harness modifier-skip census: t2 1102, t5 206, t8 80, t9 18, t10 117 — revisit cheap `info` fields at M8; baselines pinned |
| 2026-07-06T23:25:00Z | PORT | M0 CI: build-pure (opam, no libpcre2, OCaml 4.14/5.2 matrix) + conformance (nix: @all, @gen-check, runtest, runner vs baselines) | none failing → none failing | e09f684 | fuzz smoke + bench gate steps stubbed as comments until those components land; M0 COMPLETE |
| 2026-07-07T00:05:00Z | PORT | M1 chunk 0: engine driver mode — Engine.compile_ctx/info/error_message (additive skeleton), engine_driver.ml DRIVER adapter, runner --driver=oracle|engine with engine_baseline_counts.sexp, CI engine step | none failing (oracle) → testinput1:1 (engine frontier live) | 94ddd46 | engine baseline as generated (t2 has 4 driver-independent passes: harness-level syntax errors); STALE-SKIP oracle-only; rc TODO documented (offset_top/2+1 at M1 wiring) |
| 2026-07-07T00:55:00Z | PORT | M1 parse foundations: parse.ml (META scheme + meta_extra_lengths, parse_context, read_number, read_repeat_counts, check_posix_syntax/name, read_name) + errors.ml err4..err62 block | testinput1:1 → testinput1:1 (helpers not yet wired) | d9aada2 | fidelity review PASS (1 convention finding fixed: Int.equal for <>0); DEVIATION sizeoffset=1 vs C SIZEOFFSET=2 (OCaml int holds offsets in one element; meta_extra_lengths derived); read_name UTF arm deferred to M6; manage_callouts shim still owed on this checklist item |
| 2026-07-07T02:10:00Z | PORT | M1 check_escape: full escape classification (escapes table, xdigitab, ESC_ enum, \x/\o/\c/\N{U+}/\g, backslash-digit disambiguation, alt_bsux + EXTRA_ALT_BSUX arms, substitute filter) + err1..err93 block + max_non_utf_char | testinput1:1 → testinput1:1 (parse-internal) | 57c148d | fidelity review PASS, zero findings; deviations: GETCHARINCTEST byte-read (M6 deferral), cb scaffold on parse_context, COME_FROM_NU goto → hex_brace_tail fn, EBCDIC arms omitted, esc_big_* naming |
| 2026-07-07T04:00:00Z | PORT | M1 parse_regex A: main-loop skeleton — literals/PARSED_LITERAL+okquantifier, \Q\E, x/xx comments, (?#, ^ $ . arms, escape dispatch (full handle_escdsw UCP rewrite, numeric backrefs + small_ref_offset, ESC_ub, \K \R \N), ( ) | arms, (?| protocol, full inline option scan (?imnsrxJUaDPSTW/(?^, manage_callouts (full), nest stack, epilogue errors, parsed-pattern sizing | testinput1:1 → testinput1:1 | e274fd0 | review PASS, 3 convention findings fixed (citations, uint16 nest_depth wrap note, M6 Char.chr seam); 10.44 quirk flagged: META_OPTIONS extra-length table says 1, emission is 2 — compile chunks must pptr+=2 like C; deferred arms fail loudly with err 299 |
| 2026-07-07T05:20:00Z | PORT | M1 parse_regex B: character classes — open/negation/empty-class rules, ]-first literal, range state machine (RANGE_LITERAL vs ESCAPED, [a-], ERR8), POSIX items incl. [[:<:]]/[[:>:]] + UCP substitutions + EXTRA_ASCII overrides, in-class escapes via check_escape/handle_escdsw, ERR6/7/12/13/30/50/71 sites | testinput1:1 → testinput1:1 | 48720f6 | review PASS (1 finding: this log row, riding in-commit); in-class \p defers to M7 (needs get_ucp; matches freestanding \p arm policy); 36 assert cases with exact stream/offset traces |
| 2026-07-07T06:45:00Z | PORT | M1 parse_regex C: CHECK_QUANTIFIER + quantifier arms (STAR/PLUS/QUERY/MINMAX with lazy/possessive re-peek, ERR9 FAILED_BACK), {n,m} reconciliation ({,m}={0,m} per C — dispatch was wrong), named groups ((?<n> (?'n' (?P<n> DEFINE_NAME dedup/DUPNAMES/ERR43/65/41), \k//(?P= META_BACKREF_BYNAME (authorized early), test/engine init-assert force-link exe | testinput1:1 → testinput1:1 | 32369db | review PASS-equivalent (REVISE only for this log row); deviations: ERR21 malloc path N/A, META_ACCEPT wrap unreachable until M5 verbstartptr, RECURSE_BYNAME deferred; parse asserts now actually run in CI (were never linked before) |
| 2026-07-07T08:05:00Z | PORT | M1 compile utilities: compile.ml — compile_block/compile_context records, PUT/GET/PUT2/GET2 (+INC) big-endian LINK_SIZE=2 primitives, first_significant_code, find_dupname_details (ERR53), check_workspace_overflow (ERR86/ERR52), REQ_/OFLOW constants; newline_/bsr_ consts in options.ml; max_varlookbehind | testinput1:1 → testinput1:1 | de5875a | review PASS zero findings; deviations: chartables read directly (no cb pointers), memctl/stack_guard dropped, nl[4]→nl0/nl1, open_capitem is a parameter not cb field (10.44 fact, contra milestone note); find_dupname_details is the ONLY fn in 5538-5630; first_significant_code lives at 5043-5122 |
| 2026-07-07T09:35:00Z | PORT | M1 compile_branch A: head + two-pass lengthptr protocol (ERR52/86/20 sites, last_code discipline), dispatch loop, LIVE arms: ^ $ . anchors, META_OPTIONS (pptr+=2 quirk), OP_CALLOUT, literals OP_CHAR/CHARI + full firstcu/reqcu/zero* + HASCRORLF, META_ESCAPE family (\A\G\K+ERR99 \b\B UCP/ASCII_BSW \d\D\s\S\w\W \N \C+HASBKC \R \h\H\v\V \X \Z\z) | testinput1:1 → testinput1:1 | 1af744d | review PASS (1 comment-wording finding fixed); C-layout intel: classes 5860-6484=chunk B, escapes/literals 8101-8341 (doc's 5636-6500 estimate wrong); OP_NOT/NOTI belong to chunk B (negated 1-char class); \P\p and UCD_CASESET arms defer loudly |
| 2026-07-07T10:20:00Z | FIX | dev shell lost pkg-config + pcre2 C after the pure swap (package pcre2 no longer has C deps, so no depext feeds the shell); nixpkgs pkg-config wrapper also ignores ambient PKG_CONFIG_PATH — fixed by explicit buildInputs [pcre2c1044 pkg-config] (the -I/-isystem dedup concern is moot: 10.42 depext gone) | testinput1:1 → testinput1:1 | 8566dc2 | PROCESS NOTE: commit 1af744d was made while verify was red — the red was this env issue, not the chunk, but the gate failed because `echo $?` after a pipeline reads tail's status; verify commands must capture exit codes via command substitution. Clean rebuild green: oracle+engine at baseline, runtest OK |
| 2026-07-07T12:30:00Z | PORT | M1 compile_branch B: classes — empty-class OP_FAIL/OP_ALLANY, 1-char → NORMAL_CHAR_SET / OP_NOT(I), 2-char case-partner → CLASS_CASELESS_CHAR gated by UCD_CASESET (k/K s/S stay bitmaps) + EXTRA_CASELESS_RESTRICT, bitmap loop (POSIX maps + tweaks, \d\D\w\W\s\S rows, \h\H\v\V via add_(not_)list_to_class, ranges w/ fcc caseless closure), OP_CLASS/OP_NCLASS + should_flip_negation + invert-on-emit; add_to_class family; new ucd.ml (GET_UCD/CASESET/OTHERCASE) | testinput1:1 → testinput1:1 (engine driver unwired until compile_regex/driver chunks) | b8de195 | deviations: errorcodeptr threaded through add_to_class* for M6/M7 deferrals (UTF/UCP caseless closure 5246-5282, XCL emission 5322-5336, \p/\P-in-class 6243-6256, UCP posix graph/print/punct 6061-6070, OP_NOTPROP 5930-5941, utf PUTCHAR>127 — all err 299 both passes); xclass plumbing kept but dead in M1; classes at 5860-6484 (doc estimate 6500-6810 was wrong, now pinned); ~60 new module-init asserts incl. full-bitmap membership checks + two-pass length equality |
| 2026-07-07T14:10:00Z | PORT | M1 compile_branch C: repeats — quantifier head (reqvary/zero-backoff/greedy-lazy-possessive), single-char STAR..EXACT/UPTO families + {n,m} decomposition, class CR* repeats, type TYPE* repeats, possessive pass (opcode_possessify table + POS substitution + ONCE-wrap), END_REPEAT | testinput1:1 → testinput1:1 | acdbac8 | review PASS (1 test-comment citation fix); pinned: repeats are 7178-8011 not 6812-8050; bracket repeats 7424-7751 (~330 C lines) defer to chunk D; OP_RECURSE repeat to M5; 5 hand-verified byte traces incl. independent re-derivation of a{2,5} and a{2,3}+ |
| 2026-07-07T16:20:00Z | PORT | M1 compile_branch D: group arms (OP_CBRA/OP_BRA + open_capitem protocol + firstcu/reqcu group propagation), bracket repeats (BRAZERO/BRAMINZERO, {n,m} replication both passes, KETRMAX/RMIN/RPOS, BRAPOS families, ONCE wrap), minimal compile_regex (8345-8646: branch loop, ALT linking, KET chain reversal) as C-mirroring mutual recursion | testinput1:1 → testinput1:1 | cd46619 | review PASS (1 prose finding fixed); 22 patterns cross-checked vs REAL pcre2test fullbincode; reviewer independently re-derived (a|b), (a){2,4}, (a)*+ traces; add_name_to_table is driver-owned (9181-9240); KETRMAX selection lives in compile_branch not compile_regex; remaining: driver chunk owns 8650-9390 tail + 10126-11001 |
| 2026-07-07T19:30:00Z | PORT | M1 pcre2_compile driver + tail utils: pso loop, option validation (117/ERR17/92/88/74/75 precedence), newline/BSR resolution, two-pass driver (ERR20/ERR23 sites, 8 cb resets), name-table finalization (add_name_to_table), is_anchored/is_startline/find_firstassertedcu + flag words, Engine.compile/compile_ctx/info/capture_groups wired to the real pipeline | testinput1:1 → testinput1:1 (needs interpreter) but ENGINE PASSES 4 → 182 (compile-error units byte-exact: t1 21, t2 154, t5 2, t9 3, t10 2) | b99dec8 | review REVISE→fixed: reachable failwith in is_newline_at replaced with faithful PRIV(is_newline) ANY/ANYCRLF arms (pcre2_newline.c:78-145) — crash-through-boundary for (*ANY)+extended-#-comments prevented; [@tailcall] on memcmp_name; engine baseline regenerated; t2 in-scope 760→748 (newly-compiling units reclassify to harness skips) |
| 2026-07-07T21:40:00Z | PORT | M1 newline.ml: PRIV(is_newline)/PRIV(was_newline) whole-file port (ANY/ANYCRLF, both utf modes, NEL/LS/PS, boundary guards); parse.ml non-FIXED arms delegate; nltype consts move to Newline | testinput1:1 → testinput1:1 | ee2b1cf | review PASS zero findings (shared pass with frames.ml); FIXED arm stays with IS_NEWLINE macro users per C; getchar/backchar local pending utf.ml (M6 absorbs) |
| 2026-07-07T21:45:00Z | PORT | M1 frames.ml: heapframe slot layout (every C field, LP64 byte table: offsetof(eptr)=80, ovector=136), arena create (x10 floor/20480 min/KiB clamp/HEAPLIMIT-0), grow (x2, >=-at-top quirk, over_bytes clamp, KiB compare asymmetry create > vs grow >=), push copy-boundary (eptr..ovector, header excluded, rdepth+1), heap accounting in simulated C bytes | testinput1:1 → testinput1:1 | 050a84b | review PASS zero findings; reviewer hand-derived struct offsets independently; hand-worked trip points: limit 20KiB → 149 frames then -63; DEVIATION: back_frame in frame units, PCRE2_UNSET=-1, fresh arena per match (trip points unaffected) |
| 2026-07-08T01:10:00Z | PORT | M1 interpreter skeleton: match_block record, RMATCH/RRETURN as frame-arena data (NEW_FRAME with old-count match_limit compare at C:782, depth check :783), RETURN_SWITCH backtrack, OP_END full success protocol (empty-match NOTEMPTY rules, unset fill, oveccount clip), OP_CHAR + SCHECK_PARTIAL live, exhaustive stub arms + RM1..RM225 label→C-site table | testinput1:1 → testinput1:1 (exec unwired) | 76220ec | review REVISE(3)→fixed: BLOCKING unsafe_get proof hole (negative start via match_internal → OOB read) closed with pre-staged BADOFFSET check (C:6610); RM8 reattributed to OP_BRAPOS (M4); citation fix; stack-safety verified: one iterative construct, all [@tailcall], zero loop exceptions/allocation; opcodes 98-109/145-150/168 have no C top-level case (in-arm, documented) |
| 2026-07-08T03:20:00Z | PORT | M1 chars + char repeats: OP_CHARI/NOT/NOTI, EXACT/UPTO/MINUPTO/POS*/STAR..MINQUERY families (+I/NOT/NOTI), rep tables, REPEATCHAR/REPEATNOTCHAR as 18 tail-recursive fns with state in C frame slots, RM25-32 resume arms, 14 SCHECK_PARTIAL sites | testinput1:1 → testinput1:1 (exec unwired) | 058f319 | review PASS (1 header-range finding fixed); reviewer verified all order-sensitive semantics (Feptr-- before/after rrc, <= vs == guards, post-increment consultation) and every unsafe_get proof incl. arena-growth staleness (fr refetch per fn entry); auto_possess deferral means a{2,4} exercises UPTO not POSUPTO until M9 |
| 2026-07-08T05:40:00Z | PORT | M1 classes + typed repeats + anchors: OP_CLASS/NCLASS with in-arm CR* decode + RM23/24, type singles (\d\w\s \R \h\v families incl. the 0x2028 single-vs-repeat asymmetry), TYPE repeat machinery + RM33/34, ANY/ALLANY/ANYBYTE, anchors CIRC(M)/DOLL(M)/SOD/SOM/SET_SOM/EOD/EODN + \b/\B | testinput1:1 → testinput1:1 (exec unwired) | bc3d6f6 | review PASS (7 citation ranges skewed 1-3 lines, fixed); reviewer confirmed the ANYNL single-arm includes U+2028/29 while repeat loops exclude them (C 8-bit asymmetry) and RM34's CRLF double-step bounds proof; ~120 asserts oracle-verified |
| 2026-07-08T08:00:00Z | PORT | M1 brackets: BRA/CBRA/SBRA heads + GROUPLOOP (hasthen/Frdepth gate), ALT walk, KET/KETRMIN/KETRMAX (capture write, Foffset_top raise, group-frame chain pop, empty-match exits, RM6 same-frame re-iteration vs RM7), BRAZERO/BRAMINZERO/SKIPZERO + RM9/10; OP_END re-verified with live captures | testinput1:1 → testinput1:1 (exec unwired) | e7d2d05 | review PASS (2 non-blocking: this log row + branch_end comment precision, both fixed); 24 oracle-pinned gauntlet asserts incl. (a*)*b termination, ((a)|b)+ capture survival via frame copies; deferred at ket: KETRPOS/CONDASSERT/recursion/assert actions (M3-M5) |
| 2026-07-08T11:30:00Z | PORT | M1 pcre2_match driver + engine.ml wiring: option masking (-34), BADOFFSET, NOTEMPTY flag transfer (FF/OO division), anchored/firstline/startline resolution, newline switch, limit mins, bump-along loop (first_cu memchr caching, startline WAS_NEWLINE scan, start_bits, minlength, req_cu search+cache, CRLF double bump, FIRSTLINE break), per-attempt resets, rc switch (SKIP arms M5), partial bookkeeping, ENDLOOP result fill (rc=end_offset_top/2+1, startchar/leftchar/rightchar/mark); Engine.exec_full/exec/exec_captures wired; engine_driver rc from ovector-length encoding | testinput1:1 → testinput1:28 | cb04346 | ENGINE PASSES 182 → 1044 (t1 664/1290, t2 360/748, t4 9/617, t5 5/419, t9 4/10, t10 2/44); oracle baseline intact; @pure-tests 41/41 green (G1 exec half); DEVIATIONS: match_internal NOT delegated (asserts pin single-attempt semantics), Match ovector length encodes rc at the seam (exec_result has no rc field); frontier t1:28-30 all "(?=" compile-deferred err 299 → M3/M4 lookaround; No_match carries no nomatch_mark at the mli (M5 will need it) |
| 2026-07-08T10:30:00Z | GATE | G1 MET: pure OUnit 41/41 (Interp+Jit) re-attached to @runtest; engine conformance 182 → 1044 (t1 664/1290 = 51.5%, in the 45-55% band); frontier testinput1:28 (lookahead, M3); zero regressions (stash-diff); oracle baseline untouched | testinput1:1 → testinput1:28 | cb04346 | driver chunk review PASS (2 citation findings fixed in-commit); first-3 frontier failures all classified M3/M4 (assertions); residual failure classes: backrefs M2, atomic/lookbehind M4, conditionals/recursion/verbs M5, UTF M6; debug_printer follow-up open |
| 2026-07-08T13:00:00Z | PORT | M1 tail — debug_printer.ml: whole pcre2_printint.c port (all arms incl. property/verb/callout-string; CLASS_REF_REPEAT factored), Engine.print_code wired; XCL_* consts; callout delimiter tables | testinput1:28 → testinput1:28 | e3c4bcc | review PASS zero findings; 10 verbatim pcre2test -d dumps byte-identical (provenance noted); auto-possess deferral now VISIBLE in dumps (a* vs C's a*+) until M9; harness bincode/fullbincode still modifier-skipped — small harness chunk can wire CTL_FULLBINCODE later |
| 2026-07-08T15:00:00Z | PORT | M2 backrefs compile side: META_BACKREF numeric arm (OP_REF/REFI, GETPLUSOFFSET, ERR15 sites), byname arm (single-name rewrite, forward refs, OP_DNREF/DNREFI via find_dupname_details), repeat/possessive interplay verified riding chunk-C machinery unchanged | testinput1:28 → testinput1:28 (wins past frontier) | 343b635 | +17 units (t1 665, t2 376); review PASS (1 citation fix); remaining backref failures all -979 interpreter stubs → match-side chunk next; MATCH_UNSET_BACKREF compile effects live in M4 lookbehind machinery |
| 2026-07-08T17:10:00Z | PORT | M2 backrefs match side: match_ref (lcc fold, unset split mirroring C caller/callee contract), OP_REF/REFI/DNREF/DNREFI arms + REF_REPEAT decode + min/RM20/RM21-samelengths/RM22-differing maximize strategies + resumes; 3 CHECK_PARTIAL inlines | testinput1:28 → testinput1:28 (blocked on M4 lookaround) | bea38e4 | +100 units (t1 735, t2 405, t5 6; total 1165); review PASS (1 test-comment finding fixed + COVERAGE TODO for 5135-5139 branch); REFI/DNREFI utf||ucp gate loud at seam (M6/M7) |
| 2026-07-08T17:15:00Z | GATE | G2 MET: zero in-scope backref failures (full failure-set scan); testinput1 57%; M2 complete in 2 chunks (compile 343b635 + match this commit) | testinput1:28 → testinput1:28 | bea38e4 | next: 04-lookaround-atomic-possessive.md (the frontier's blocker); MATCH_UNSET_BACKREF lookbehind-length effects ride M4; possessive-ref conformance unit t2:821 waits on OP_ONCE ket (M4) |
| 2026-07-08T19:40:00Z | PORT | M3 lookaround/atomic parse+compile: symbolic + alpha assertion arms (alasmeta), OP_ASSERT*/ONCE bravalues, (?!)→OP_FAIL gate, compile_regex REVERSE/VREVERSE insertion, parsed_skip/get_grouplength/get_branchlength/set_lookbehind_lengths/check_lookbehinds (ERR125/187/100 machinery incl. MATCH_UNSET_BACKREF sites), driver call site | testinput1:28 stays (mode: err 299 → -979 interpreter stub, predicted) | 8661ca3 | +35 units (t1 745, t2 430; total 1196); t2:1402 reclassified to harness-skip (offset_limit modifier now reachable); review PASS (2 mechanical findings fixed); groupinfo exact-size deviation; next: interpreter OP_ASSERT*/ONCE/RM3-4/ket actions |
| 2026-07-08T21:50:00Z | PORT | M3 interpreter: assert arms + RM3/RM4 (ACCEPT frame blit protocol, negative inversion, COMMIT/SKIP/PRUNE pass-back), OP_REVERSE/VREVERSE + RM37 (max-first descent), ONCE ket atomic discard (back_frame chain surgery, frame units), BRAPOS families + KETRPOS committed-iteration protocol + RM8 | testinput1:28 → testinput1:250 | f4f547d | +226 units (t1 934/1290 = 72.4%, t2 467; total 1422); review PASS zero findings; capture rollback/persistence rules oracle-pinned; remaining near-frontier: conditionals 299 (parse deferral, M5 doc), OP_FAIL dispatch 3-liner rides M5 verbs, verbs-in-asserts ready |
| 2026-07-08T21:55:00Z | GATE | G3 MET (reconciled): all 04-doc chunks done (possessive-quantifier compile checkbox reconciled to M1 chunks C/D); t1 72.4% vs ~80% estimate — residual is entirely M5-class + UTF units; zero in-scope M3 failures | testinput1:250 | f4f547d | next: 05-conditionals-recursion.md |
| 2026-07-09T00:20:00Z | PORT | M4 conditionals+recursion parse/compile: (?( all condition forms (number/relative/name/R/Rn/R&name/DEFINE/VERSION/assert) with expect_cond_assert protocol, recursion forms (?R (?n (?±n (?&name (?P> \g<> → META_RECURSE(_BYNAME), OP_COND/SCOND + CREF/DNCREF/RREF/DNRREF/FALSE/TRUE emission, DEFINE rewrite, OP_COND post-processing (ERR27/54 stale-offset parity), OP_RECURSE emission + find_recurse + driver fixup loop (RSCAN cache) | testinput1:250 stays (mode: 299 → -979 stub, predicted) | 64ef0c8 | +82 units (t1 937, t2 546; t2:1840 reclassified to match_limit harness-skip); review PASS (2 findings: stale comment fixed, this log row); quantified recursion + callout-condition still loud-deferred |
| 2026-07-07T12:50:00Z | PORT | M4 conditionals+recursion match: OP_COND/OP_SCOND condition dispatch (CREF/DNCREF/RREF+RREF_ANY/DNRREF/FALSE/TRUE) + assertion-condition protocol (RM5, GF_CONDASSERT ket return), OP_RECURSE + RM11 branch loop + RECURSELOOP −52 with DISABLE_RECURSELOOP_CHECK gate, recursion ket actions (whole-pattern OP_BRA end + CBRA capture reinstate = Perl discard semantics), OP_FAIL, quantified-recursion compile case (replication + OP_BRA wrap + repeat_bracket_group fallthrough factoring) | testinput1:250 → testinput1:831 | 4e80476 | +209 units, zero regressions (per-unit set diff; t1 1078/1290 = 83.6%, t2 614); oracle-pinned module asserts (both cond paths, capture-discard, −52 trio, −53/−63 under disabled loop check, quantified forms); OP_CLOSE/OP_ACCEPT (incl. in-recursion ACCEPT walk over GF_RECURSE frames, pcre2_match.c:854-874) stay M5-verbs; new frontier region = verbs 299 deferral (t1:831-856, t1:1168 blocked only by embedded (*COMMIT) at offset 2569); callout-condition units (t2:1099/1100/1364/1520/1533-1536/1607) ride M8 |
| 2026-07-09T03:10:00Z | GATE | G4 MET: conditionals+recursion complete (OP_COND/SCOND + assertion conditions + GF_CONDASSERT ket, OP_RECURSE + RECURSELOOP + capture reinstate protocol, OP_FAIL, quantified recursion) | testinput1:250 → testinput1:831 | 4e80476 | +209 units (t1 1078/1290 = 83.6%, t2 614/746; total ~1713); review PASS zero findings; frontier region t1:831-856 = verbs (06 doc); recursion-verb combos ready on the recursion side |
| 2026-07-09T05:40:00Z | PORT | M5 verbs parse+compile: verbs table, verb arm (arg dispatch, pre-MARK conversion, verbstartptr), inverbname block (escape rules, ERR40/76 sites), CHECK_QUANTIFIER ACCEPT wrap now live, compile arms OP_MARK..OP_THEN_ARG + ACCEPT CLOSE-walk + ASSERT_ACCEPT selection + HASTHEN/HASACCEPT + had_pruneorskip consumers active | testinput1:831 → testinput1:833 (mode shift to -979 stubs) | 20c9277 | +43 units (t1 1096, t2 635, t9 8); review PASS zero findings; \K confirmed fully shipped in M1; (*LIMIT_*) already live via pso; next: interpreter verb dispatch + MATCH_COMMIT/SKIP/PRUNE/THEN driver plumbing |
| 2026-07-07T00:00:00Z | PORT | M5 verbs match: mb verb fields (verb_ecode_ptr/verb_skip_ptr/verb_current_recurse/skip_arg_count/ignore_skip_arg), verb dispatch arms OP_MARK..OP_THEN_ARG + RM12-RM19/RM36 resumes (SKIP_ARG→MARK interception, ignore_skip_arg no-op replay), MATCH_THEN branch-scope checks in RM2/RM8/RM11 (+ recursion verb-range NOMATCH), OP_CLOSE/OP_ACCEPT (in-recursion GF_RECURSE walk; OP_END tail factored+shared), driver rc switch (SKIP new-start, SKIP_ARG re-run, PRUNE/THEN bump, COMMIT stop) + skip_arg_count per-attempt reset; Engine.No_match now carries nomatch mark (mli + engine_driver); mark_of_offset fixed to mark[-1] length protocol (embedded-NUL names) | testinput1:833 → testinput2:1096 | 32d6540 | +283 units, zero regressions (per-unit set diff; t1 1290/1290 = 100%, t2 722/746, t9 10/10); oracle-pinned module asserts (frontier trio, COMMIT bump-suppression pair, MARK both-paths + nomatch mark, PRUNE/THEN classic, SKIP new-start + name-match + unmatched fallback + recursion escape, ACCEPT-in-recursion both shapes, OP_CLOSE ovector, first-cu/req-cu no-attempt no-mark cases); remaining t2 failures all (?C callouts (M8); G5 t1 gate condition met on the match side, study/partial/harness chunks remain |
| 2026-07-09T08:20:00Z | GATE | G5 MET: verbs complete — t1 1290/1290 (100%), t2 722/746 (96.8%), t9 10/10; partial-matching + basic-study + harness checkboxes reconciled to their M1/G0 shipping chunks | testinput1:833 → testinput2:1096 | 32d6540 | review PASS (1 finding: embedded-NUL mark fix guarded at conformance layer by t2:1482-1485+1782 in the engine baseline, noted in lieu of a module assert); remaining t2 failures all M8 callouts; next: 07-utf8.md |
| 2026-07-09T12:30:00Z | PORT | M6 UTF-8 compile side: valid_utf.ml (all UTF8_ERR1..21 offset-exact), utf.ml primitives (GETCHAR family, ord2utf, ACROSSCHAR-as-loop), compile gate live, all (* M6 *) parse/compile sites un-deferred (caseless closure via get_othercase_range, XCLASS emission, mcbuffer literals, VERB_ARG, read_name UTF, MAYBE_UTF_MULTI range proof) | testinput2:1096 (unchanged; UTF corpora behind it) | c38a8a0 | +47 units (t4 13, t5 38, t10 13); review REVISE→fixed: BLOCKING NO_UTF_CHECK crash (truncated tail decode + UCD overrun) closed with byte-clamped peek (C NUL-read precedent) + record_index clamp (proved ≡ C dummy-record guard); pinned behavior verified byte-identical to oracle where reachable; Utf.backchar prefix-walk flagged for the match-arms executor |
| 2026-07-09T16:40:00Z | PORT | M6 UTF-8 match arms: driver subject validation (-36 vs UTF8_ERR20 split, startchar propagation via Error{code;start_char} seam extension), xclass.ml (whole pcre2_xclass.c), UTF variants of char/class/type/repeat/boundary/lookbehind arms + RM100/101/200-207/219/221, ord2utf into occu frame slots, bump-along ACROSSCHAR | testinput2:1096 (M8 callout ahead in order) | b60a914 | +460 units (t4 282/617, t5 215/419, t10 27/44; total 2546); review REVISE→fixed: BLOCKING xclass closure-per-char allocation hoisted to module-level scan_items/map_bit (zero-capture); clamped subject reads resolve the backchar prefix-walk note; MATCH_INVALID_UTF fragment loop deferred loudly (17 t10 units) — follow-up chunk next; residual census: \p/\X/script-run (08), callouts (M8), zero bug-class |
| 2026-07-09T19:10:00Z | GATE | G6 MET: UTF-8 complete — MATCH_INVALID_UTF fragment machinery (per-fragment NOTBOL/NOTEOL, terminal-only partials, ^-never-at-fragment-start), t10 44/44 (100%); t4/t5 residual = \p/\X/script-run only | testinput2:1096 | c46b74b | +17 units (total 2563); review PASS zero findings (reviewer proved end_subject local≡field at every ENDLOOP entry); doc 07 closed except the fuzz item (doc 09); next: 08-ucp.md |
| 2026-07-09T21:30:00Z | PORT | M7 UCP compile side: get_ucp (loose matching, sc:/scx:/bc: prefix splits, binary chop over utt, ERR46/47), freestanding + in-class \p/\P parse arms, OP_PROP/NOTPROP/ALLANY compile arm, XCL_PROP producers + UCP posix graph/print/punct XCL shapes | testinput2:1096 | 99af66b | +31 units (t4 289, t5 239; total 2594); review PASS zero findings; match arms next (OP_PROP stubs + Xclass prop_unported + extuni + UCP boundaries consume the (ptype,pdata) now emitted); ucd.ml needs script/gbprop/bprops/scriptx accessors |
| 2026-07-10T01:00:00Z | PORT | M7 UCP match side: ucd.ml accessors (script/gbprop/bidiclass/scriptx split/bprops bitmaps), prop_test zero-closure fn (all PT cases incl. PXGRAPH Cf exceptions), OP_PROP arms + prop repeat machinery (RM208-217/222-225), UCP word boundary, Xclass XCL_PROP arms (bool-return refactor) | testinput2:1096 | a56e75f | +449 units (t4 581/617, t5 396/419; total 3043); review PASS (2 comment findings fixed: ucd clamp claim qualified re bidiclass/bprops dummy divergence, hspace citation); split: \X + script runs + ucp-caseless singles → final 08 chunk; fmt fixup for 99af66b files folded in |
| 2026-07-10T04:30:00Z | GATE | G7 MET (modulo 2 callout-owned t5 units): \X graphemes (extuni.ml, GB9/9a/11 + RI evenness), script runs (script_run.ml, require_map machine + Han combos + digit-set rule), (*sr:/(*asr: (atomic = ONCE inside SCRIPT_RUN — C nesting verified, my earlier description was inverted), ucp-caseless singles; t4 617/617 (100%), t5 417/419 | testinput2:1096 | ac04244 | +57 units (total 3100/3112); review PASS zero findings; ALL 26 remaining failures callout-blocked; study/auto-possess chunks must include OP_SCRIPT_RUN/OP_EXTUNI case arms (noted for M9); next: callouts |
| 2026-07-07T00:00:00Z | PORT | M8 callouts (compile + no-op execution, LAST feature chunk): (?Cn)/(?C"text") parse arm (pcre2_compile.c:4449-4563: expect_cond_assert decrement, auto-callout abolish, delimiter tables, ERR38/39/81/82 + ERR14/72 sites), META_CALLOUT_STRING compile arm (7114-7175: pre-pass estimate, doubled-delimiter copy, item-length PUT, usedlength slack), do_callout shell (pcre2_match.c:254-334 → length-only, callout return pinned 0 = mb->callout NULL at C:283), OP_CALLOUT/OP_CALLOUT_STR dispatch (5590-5600) + OP_COND callout skip with Flength adjust (5623-5638); err38/39/72/81/82 constants | testinput2:1096 → none (all in-scope units pass, all 7 files) | fd6ab32 | +12 passing (t2:1299-1302,1353,1354,1364,1520,1533,1535,1536 + t5:419), 14 units reclassify to harness modifier-skips exactly matching the oracle (t2:1096-1101,1307,1554,1605-1607,1763 callout_capture/error, 1647 callout_extra, t5:510) — engine/oracle skip sets diff-identical (1531 rows); DECISION: pcre2test default-callout trace NOT implemented (out-of-scope per plan; the 7 trace-demanding units stay skiplisted, zero skiplist edits, oracle baseline untouched); engine baseline t2 722/746→733/733, t5 417/419→418/418; module asserts pinned vs real pcre2test 10.44 (parsed streams incl. auto_callout abolish interplay, OP_CALLOUT_STR bytes incl. {ab}}c} doubled-delim, all 7 error offsets) |
| 2026-07-10T08:00:00Z | GATE | FULL CONFORMANCE PARITY: engine == oracle on every in-scope unit, all 7 files (t1 1290, t2 733, t4 617, t5 418, t8 0, t9 10, t10 44 = 3112/3112); skip sets diff-identical (1531 rows); frontier: none | testinput2:1096 → none | fd6ab32 | last feature chunk (callouts) landed; G8's conformance half met — the fuzz campaign (1M cases) is G8's remaining half; then M9 auto-possess/study (behavior-neutral) + M10 bench/perf gate |
| 2026-07-10T12:30:00Z | PORT | M9 auto_possess.ml: whole pcre2_auto_possess.c (4 compatibility tables cell-exact, compare_opcodes recursion incl. had_recurse/VREVERSE refusals + chr<255 WORDCHAR quirk, get_chr_property_list with PT_CLIST conversion, rec_limit=1000 CVE guard, main scan with POS* deltas), driver call before study, NO_AUTO_POSSESS gate, ERR80 | none → none (behavior-neutral gate held: zero conformance delta, per-unit sets identical) | e6630aa | review PASS zero findings (all ~700 table cells verified); debug dumps now byte-identical to pcre2test -d incl. possessified forms; 7 new oracle dump pins; build gated via @src/@test/@oracle/@gen aliases (fuzz/ WIP by parallel agent breaks @all transiently) |
| 2026-07-10T17:30:00Z | PORT | M9 study.ml: whole pcre2_study.c (find_minlength with backref cache + recursion cycle guards, set_start_bits SSB machine with the 72-entry fail list incl. OP_MARK, set_table_bit caseless/UTF rules, PRIV(study) driver + caseless first-cu pair promotion), driver call wired, interpreter start-opt reads live | none → none (zero conformance delta) | dfa52ee | review PASS (2 mechanical findings fixed); the 4 fuzz divergences (all missing-study start-opt artifacts: nomatch-mark over-report, recurse-loop vs nomatch, phantom partial, partial ovector start) flipped DIVERGE→OK live; study.c:878 missing-cbit_type upstream quirk copied faithfully |
| 2026-07-10T17:35:00Z | PORT | fuzz/fuzz_diff.exe: grammar fuzzer over full feature space (patterns/subjects incl. invalid UTF/option draws minus NO_UTF_CHECK), ddmin shrink, pcre2test-format repros; runner --regressions replay + auto-replay; CI fuzz smoke 10k + regression steps | none → none | bfe416b | found 4 real divergence classes (150k+ cases), all verified vs real pcre2test, all resolved by study; 4 minimized repros = permanent regression guards; rate 794-2997 cases/sec; G8 fuzz half: 150k clean, 1M campaign viable nightly |
| 2026-07-10T22:00:00Z | PORT | M10 bench suite: bench.ml (12-benchmark corpus a-h, interleaved engine/oracle/raw-C, median-of-5, correctness cross-checks, Gc counters, --quick), compare.ml (geomean + per-bench ≤2.0 gate), raw_c_stubs.c honesty number, bench.yml nightly workflow | none → none | 35c6fb6 | FIRST PERF NUMBERS — GATE FAIL: geomean 46.9x (range 7.6x-95.4x). Profile: dispatch floor 7.6x (pathological; frame loop confirmed zero-alloc, 244 words/10M ticks); dominant cost = ~66 minor words PER ATTEMPT (closure nest inside match_ allocated per attempt; email 2.6GB minor heap/rep). All 12 correctness cross-checks PASS incl. same -47 MATCHLIMIT. Plan: (1) hoist closure nest → per-exec/zero alloc; (2) grind dispatch floor. Fuzz 1M campaign: 1 crash find (204387 repro) — FIX dispatched |
| 2026-07-08T17:50:00Z | FIX | fuzz crash 204387-b7915f8f: was_newline OOB BACKCHAR walk — C 10.44 (and current upstream master) reads before the subject when all code units before pos are UTF-8 continuation bytes, reachable only via MATCH_INVALID_UTF's bad-start skip (pcre2_match.c:6829-6851 vs the mb->start_subject guard at 6962/7318-7349/6206); genuine C UB, confirmed by ASan + guard-page probe on the vendored sources. Fix: inline bounded BACKCHAR in newline.ml was_newline — walk stops at position -1, reads 0 there (the LE oracle's deterministic block-header byte), DEVIATION documented; deliberately NOT clamp-at-0 (would misread a leading NEL continuation byte under NLTYPE_ANY). Repro pinned: engine.ml module-init assert (exact pattern/options/subject → No_match{mark=None}) + 3 was_newline probe asserts incl. the clamp-vs-pin discriminator; repro file committed to fuzz/corpus/regressions/ | none → none | e8eeaa8 | first attempt was killed mid-work leaving an unverified tangle — tree reset to 81269db and redone cleanly per REMAINING-WORK §0/§2.1; review PASS (2 non-blocking doc findings fixed: dead backchar alias removed, DEVIATION determinism claim scoped to the LE host); full battery green (both drivers at baseline 3112, frontier none, regressions 5/5 OK, fuzz smoke 10k clean, pure build OK); upstream-reportable C bug; next: 200k×3-seed + 1M seed-42 campaigns → mark G8 |
| 2026-07-08T19:30:00Z | FIX | seed-101 fuzz campaign SIGSEGV (case 153814, bisected with the prebuilt binary; kernel log placed the fault in libpcre2-8.so): SECOND distinct upstream C bug — 8-bit GET_UCD has no bounds guard (32-bit build guards at MAX_UTF_CODE_POINT, pcre2_internal.h:1869-1873), so invalid-UTF decode under MATCH_INVALID_UTF (0xff lead → c=0x60000000 via pcre2_match.c:2221 GETCHARINCTEST) over-indexes the UCD stage tables ~25MB (pcre2_xclass.c:137, ASan-proven). Engine already safe/deterministic (ucd.ml record_index clamp DEVIATION). Resolution: ORACLE adopts the engine's documented UB pins — (1) dev-only patch on the flake's pcre2c1044 derivation clamping GET_UCD to MAX_UTF_CODE_POINT (clamp ≡ ucd.ml record-for-record incl. bidiclass/bprops; deliberately NOT the 32-bit dummy-record form), patch file at oracle/patches/, vendor/ pristine; (2) oracle stubs pad pattern+subject with 8 zero slack bytes each side (forward GETCHAR-past-end family pcre2_match.c:2221 + backward BACKCHAR family from e8eeaa8 read deterministic 0x00; keeps the 204387 pin valid); (3) ucd.ml DEVIATION comment updated (also reachable under MATCH_INVALID_UTF); (4) new fuzz_diff --dump-case N mode (RNG-faithful, no-execution); (5) regression corpus 153814-ucdclamp.txt = (?<=h{,2}\p{Greek}) vs \xff\x60, replays green | none → none | 13454e2 | full differential coverage preserved — NO fuzzer exclusions; review PASS (4 non-blocking findings fixed: stub OOM abort, corpus header format ×2, flake comment scope); battery green incl. seed-101 200k (was exit 139 → clean) and seed-42 200k re-check; baselines untouched; stale-link trap noted: dune clean required after flake change (discover.exe caches c_library_flags); SECOND upstream-reportable C bug; next: fresh-seed 200k + 1M seed-42 → mark G8 |
| 2026-07-08T20:30:00Z | FIX | seed-20260708 fuzz campaign GPF (kernel: ip in fuzz_diff.exe = oracle_test_exec+0x229, pcre2test_stubs.c:178 mark[-1]): dev-only STUB bug, predates all engine work (same-shape GPF at 01:55 in the pre-session binary) — pcre2_match_data_create leaves match_data->mark UNINITIALIZED (pcre2_match_data.c:57-72) and pcre2_match assigns it only in the post-loop epilogue (pcre2_match.c:7706/7741, limit errors via default: goto ENDLOOP 7575-7576); the early validation returns (6597-6619, 6666, 6673, BADUTFOFFSET 6839, UTF8_ERR20 6841, valid_utf 6902) leave heap garbage → occasional wild pointer → GPF on mark[-1], heap-state-dependent (why only a fresh seed hit it). Fix: oracle_test_exec reads mark only for rc >= 0 / NOMATCH / PARTIAL (exactly pcre2test's consumed set, pcre2test.c:8127-8166/8258-8263), else None; startchar audit (zeroed at 6688; consumed rcs all defined) documented, read unchanged; pcre2_c_stubs.c audited clean (no mark/startchar reads, ovector only for ret > 0) | none → none | pending | review PASS (2 comment-precision findings fixed inline by orchestrator: non-JIT citation lines 6839/6841/6892/6901 vs the unreachable SUPPORT_JIT copies 6716/6756-6760, and 6666/6673 added to the startchar early-return list — verified against vendored source); battery green, 6/6 regressions, baselines untouched; campaigns post-fix: 200k×{42,101,20260708} all crash-free, BUT seed-20260708 case 125828 surfaces a genuine ENGINE capture-retention divergence previously masked by the GPF (optional lookbehind group ending in (*ACCEPT) inside a lookahead keeps groups 1-2 = (0,1); C leaves them unset) — separate FIX next, blocks G8 |
